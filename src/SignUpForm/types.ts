export type State = {state_name: string};
export type City = {city_name: string};

export type UserData = {
    firstName: string;
    lastName: string;
    country: string;
    city: string;
    email: string;
    password: string;
};
